<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jxau.li.mapper.MerchantApplianceInfoMapper">

    <update id="updateStatus">
        update appliance_info set status = #{status} where id = #{id}
    </update>
    <update id="updateIsOrdered">
        update appliance_info set is_ordered = #{isOrdered} where id = #{id}
    </update>
</mapper>